//use this to create classes for the group studio project

class Studio {
  constructor (title, cost, size, days) {
    this.title = title;
    this.cost = cost;
    this.size = size;
    this.days = ["Mon", "Tue", "Wed", "Thu", "Fri", "Sat", "Sun"];
  }
}
